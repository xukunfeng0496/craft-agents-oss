# 0.10.317

## 修复
- **Windows 下技能市场登录、CVTE 门户登录卡死**：登录时浏览器未弹出、界面一直停在「正在登录」无下文。根因是登录链路在渲染进程打开浏览器受系统限制而静默失效；现改由主进程打开门户页面，并为回调等待加入超时保护（失败时明确报错而非一直转圈）。

## 变更
- 安装包文件名统一为 **Work-Agent**（与应用品牌一致，替换旧的 Craft-Agents 命名）。
