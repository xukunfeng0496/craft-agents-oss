export function getPermissionsTableColumnTitles(headers: {
  access: string
  type: string
  pattern: string
  comment: string
}) {
  return headers
}

export function getLabelsTableColumnTitles(headers: {
  color: string
  name: string
  type: string
}) {
  return headers
}
